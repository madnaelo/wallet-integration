# Swap Aggregator MVP (0x + MetaMask)

Production-ready MVP using:
- Next.js (TypeScript, App Router)
- ethers.js
- MetaMask (EIP-1193)
- Backend: Next.js Route Handler `/api/quote` calling 0x Swap API

## Setup

